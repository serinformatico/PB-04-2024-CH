/*
    Ejemplo de uso de callback dentro de la función map de JS.
*/

const numeros = [10, 20, 30];
const numerosIncrementadosEn2 = numeros.map((numero) => numero + 2);

console.log("\nEJEMPLO: Uso de callback dentro de la función map de JS");
console.log(numerosIncrementadosEn2);